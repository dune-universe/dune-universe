(library
 ((name        expect_test_helpers)
  (public_name expect_test_helpers)
  (flags (:standard -safe-string))
  (libraries (
    core
    async
    expect_test_helpers_kernel
    sexp_pretty
    ))
  (preprocess (pps (ppx_jane ppx_driver.runner)))))


(jbuild_version 1)
