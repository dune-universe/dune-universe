(library
 ((name core_profiler_disabled)
  (public_name core_profiler.disabled)
  (flags (:standard -safe-string))
  (libraries (core))
  (preprocess (pps (ppx_jane ppx_driver.runner)))))

(jbuild_version 1)
