(library
 ((name email_address)
  (public_name email_message.email_address)
  (flags (:standard -safe-string))
  (libraries (
    angstrom
    core_kernel
    ))
  (preprocess (pps (ppx_jane ppx_driver.runner)))
  ))

(jbuild_version 1)
