(library (
  (name email_message_test)
  (flags (:standard -safe-string))
  (libraries (async core email_message expect_test_helpers))
  (preprocess (pps (ppx_jane ppx_driver.runner)))
))

(jbuild_version 1)
