(jbuild_version 1)

(executables
 ((names     (test speed))
  (libraries (crlibm benchmark))))

(alias
 ((name runtest)
  (deps (test.exe))
  (action (run ${<}))))

(alias
 ((name speed)
  (deps (speed.exe))
  (action (run ${<}))))
