(library
 ((name topological_sort)
  (public_name topological_sort)
  (flags (:standard -safe-string))
  (libraries (base stdio))
  (preprocess (pps (ppx_jane ppxlib.runner)))
))


(jbuild_version 1)
