(library
 ((name mlt_parser)
  (public_name mlt_parser)
  (libraries (
    core_kernel
    ppx_expect.matcher
    compiler-libs.common
    pcre
    ppx_expect.payload
    ppxlib
    ))
  (preprocess (pps (ppx_jane ppxlib.runner)))))

(jbuild_version 1)
