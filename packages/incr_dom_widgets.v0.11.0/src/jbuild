(library
 ((name incr_dom_widgets)
  (public_name incr_dom_widgets)
  (preprocess (pps (js_of_ocaml-ppx ppx_jane -ite-check ppxlib.runner)))
  (flags (:standard -safe-string))
  (libraries (
    async_kernel
    async_js
    js_of_ocaml
    incr_dom
    incremental_kernel
    incr_map
    incr_select
    record_builder
    splay_tree
  ))
))


(jbuild_version 1)
