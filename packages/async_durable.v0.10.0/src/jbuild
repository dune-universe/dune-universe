(library
 ((name async_durable)
  (public_name async_durable)
  (preprocess (pps (ppx_jane -check-doc-comments ppx_driver.runner)))
  (flags (:standard -safe-string))
  (libraries
   (async_kernel
    async_rpc_kernel
    core_kernel))))


(jbuild_version 1)
