(library
 ((name ppx_xml_conv)
  (public_name ppx_xml_conv)
  (kind ppx_deriver)
  (ppx_runtime_libraries (csvfields))
  (flags (:standard -safe-string))
  (libraries (base ppxlib ppx_conv_func ppx_fields_conv))
  (preprocess (pps (ppxlib.metaquot ppxlib.runner)))))


(jbuild_version 1)
