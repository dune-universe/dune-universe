(jbuild_version 1)

(library
 ((name        dokeysto)
  (public_name dokeysto)
  (modules (db utls))
  (libraries (unix extunix lz4))))

(executable
  ((name test)
   (modules (test))
   (libraries (dokeysto))))
