(library
 ((name posixat_test)
(flags (:standard -safe-string))
  (libraries (posixat
              shexp.bigstring-io
              core
              expect_test_helpers_kernel))
  (preprocess (pps (ppx_jane ppx_driver.runner)))))

(jbuild_version 1)
