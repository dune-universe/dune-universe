include ../../../Makefile.monolith
EXPECTING_BUGS := 1
# Problem: it can take a minute to find a counterexample.
# Random mode is usually more efficient than afl-fuzz.
TIMEOUT   := 60
SEED_SIZE := 1000
MODE := random
