(library (
  (name async_extended)
  (public_name async_extended)
  (flags (:standard -safe-string))
  (libraries (
    async
    async_find
    async_inotify
    async_interactive
    async_shell
    command_rpc
    core
    core_extended
    delimited_parsing
    expect_test_helpers
    textutils
  ))
  (preprocess (pps (ppx_jane ppx_driver.runner)))
))

(jbuild_version 1)
