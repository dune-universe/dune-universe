(library
 ((name incremental_kernel)
  (public_name incremental_kernel)
  (libraries (core_kernel))
  (preprocess (pps (ppx_jane -check-doc-comments ppx_driver.runner)))
  (preprocessor_deps (debug.mlh))
  (flags (:standard -safe-string))
))


(jbuild_version 1)
