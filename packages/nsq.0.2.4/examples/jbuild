(jbuild_version 1)

(executables
 ((names (example consume publish))
  (libraries (nsq logs logs.fmt fmt.tty))))

(install
 ((section bin)
  (files (
    (example.exe as example)
    (consume.exe as consume)
    (publish.exe as publish)))))