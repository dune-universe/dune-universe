(executables (
  (names (bench_sexprep))
  (libraries (
    bin_prot
    core
    core_extended
    sexplib
    typerep_experimental
  ))
  (preprocess
    (pps (
      ppx_type_conv
      ppx_sexp_conv
      ppx_bin_prot
      ppx_typerep_conv
     ppx_driver.runner)))
))

(jbuild_version 1)
