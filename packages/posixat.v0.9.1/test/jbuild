(library
 ((name posixat_test)
  (libraries (posixat
              shexp.bigstring-io
              core
              expect_test_helpers_kernel))
  (preprocess (pps (ppx_jane ppx_driver.runner)))))

(jbuild_version 1)
