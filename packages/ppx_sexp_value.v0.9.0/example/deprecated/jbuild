
(library
 ((name example_ppx_sexp_value_deprecated_lib)
  (libraries (core))
  (flags (:standard -w -22))
  (preprocess (pps (ppx_sexp_value ppx_driver.runner)))))

(alias ((name DEFAULT) (deps (example.ml.pp))))

(jbuild_version 1)
