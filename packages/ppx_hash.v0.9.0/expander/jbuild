(library
 ((name ppx_hash_expander_lib)
  (public_name ppx_hash.expander)
  (ppx_runtime_libraries (ppx_hash.runtime-lib))
  (libraries (ppx_core compiler-libs.common))
  (preprocess (pps (ppx_metaquot ppx_driver.runner)))))

(jbuild_version 1)
