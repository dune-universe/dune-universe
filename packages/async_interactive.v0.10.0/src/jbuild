(library (
  (name async_interactive)
  (public_name async_interactive)
  (flags (:standard -safe-string))
  (libraries (
    async
    core
  ))
  (preprocess (pps (ppx_jane ppx_driver.runner)))
))

(jbuild_version 1)
