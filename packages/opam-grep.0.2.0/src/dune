(executable
 (name main)
 (public_name opam-grep)
 (libraries
  progress
  cmdliner
  fpath
  bos))

(rule
 (target config.ml)
 (action
  (with-stdout-to %{target}
   (progn
    (echo "let version = {|%{version:opam-grep}|}\n")))))
