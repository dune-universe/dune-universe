(jbuild_version 1)

(library
 ((name ocaml_monadic)
  (public_name ocaml-monadic)
  (synopsis "Monad syntax ppx")
  (libraries (compiler-libs.common
              ocaml-migrate-parsetree))
  (kind ppx_rewriter)
  (preprocess (pps (ppx_tools_versioned.metaquot_406)))))
