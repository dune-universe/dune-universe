(library
 ((name        expect_test_helpers_kernel)
  (public_name expect_test_helpers_kernel)
  (libraries (
    core_kernel
    re.pcre
    sexp_pretty
    ))
  (preprocess (pps (ppx_jane ppx_driver.runner)))
  ))


(jbuild_version 1)
