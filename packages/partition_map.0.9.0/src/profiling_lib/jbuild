(jbuild_version 1)

(library (
  (name profiling_lib)
  (synopsis "Common profiling code.")
  (libraries (partition_map lib08 lib09))
  (preprocess (pps (bisect_ppx -conditional)))
  (ocamlopt_flags -O3)
  ))
