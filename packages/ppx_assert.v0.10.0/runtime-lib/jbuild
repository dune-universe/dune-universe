(library
  ((name ppx_assert_lib)
   (public_name ppx_assert.runtime-lib)
  (flags (:standard -safe-string))
   (libraries (base base.caml sexplib))
   (preprocess (pps (ppx_type_conv ppx_sexp_conv ppx_driver.runner)))
))

(jbuild_version 1)
