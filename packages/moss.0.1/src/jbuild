(jbuild_version 1)

(library
 ((name        moss)
  (public_name moss)
  (flags       (:standard -safe-string))
  (synopsis "Client for the MOSS plagiarism detection service")
  (libraries (unix bytes uri))))
