(library
 ((name        csvfields)
  (public_name csvfields)
  (flags (:standard -safe-string))
  (libraries
   (core
    csvlib
    sexplib
    xml_light
    num))
  (preprocess (pps (ppx_jane ppx_driver.runner)))))

(jbuild_version 1)
