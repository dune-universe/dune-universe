(library
 ((name        ppx_dryunit)
  (public_name ppx_dryunit)
  (kind ppx_rewriter)
  (libraries (ocaml-migrate-parsetree ppx_tools_versioned unix str))
  (preprocess (action (run ${bin:cppo} -V OCAML:${ocaml_version} ${<})))
))
