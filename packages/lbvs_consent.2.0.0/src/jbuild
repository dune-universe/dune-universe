(jbuild_version 1)

(library
 ((name        lbvs_consent)
  (public_name lbvs_consent)
  (modules (consent fingerprint MyIntSet MyUtils score flags score_label
            ROC MyList Top_keeper Molecule Sdf Pair Consensus Policy Mop2d_env Mol2 Mini_mol Node Atom Bond))
  (libraries (dolog batteries bitv parmap str zip minivpt cpm))))

(executables
 ((names   (scons))
  (public_names (lbvs_consent))
  (modules (scons))
  (libraries (lbvs_consent batteries dolog bitv parmap str zip minivpt cpm))))

(executables
 ((names   (pcons))
  (modules (pcons))
  (libraries (lbvs_consent batteries dolog bitv parmap str zip minivpt cpm))))

(executables
 ((names   (filter))
  (modules (filter))
  (libraries (lbvs_consent batteries dolog bitv parmap str zip minivpt cpm))))

(executables
 ((names   (mine_BM_frameworks))
  (modules (mine_BM_frameworks))
  (libraries (lbvs_consent batteries dolog bitv parmap str zip minivpt cpm))))

(executables
 ((names   (sdf_reader))
  (modules (sdf_reader))
  (libraries (lbvs_consent batteries dolog bitv parmap str zip minivpt cpm))))

(executables
 ((names   (unit_tests))
  (modules (unit_tests))
  (libraries (lbvs_consent batteries dolog bitv parmap str zip minivpt cpm oUnit qcheck))))

(executables
 ((names   (sampler))
  (modules (sampler))
  (libraries (lbvs_consent batteries dolog bitv parmap str zip minivpt cpm))))

(executables
 ((names   (ic50_doctor))
  (modules (ic50_doctor))
  (libraries (lbvs_consent batteries dolog bitv parmap str zip minivpt cpm))))

(executables
 ((names   (pot_scale_doctor))
  (modules (pot_scale_doctor))
  (libraries (lbvs_consent batteries dolog bitv parmap str zip minivpt cpm))))

(executables
 ((names   (dist_matrix))
  (modules (dist_matrix))
  (libraries (lbvs_consent batteries dolog bitv parmap str zip minivpt cpm))))

(executables
 ((names   (filter_by_inchikey))
  (modules (filter_by_inchikey))
  (libraries (lbvs_consent batteries dolog bitv parmap str zip minivpt cpm))))

(executables
 ((names   (mop2d_encoder))
  (public_names (lbvs_consent_mop2de))
  (modules (mop2d_encoder))
  (libraries (lbvs_consent batteries dolog bitv parmap str zip minivpt cpm))))

(executables
 ((names   (mop2d_indexer))
  (public_names (lbvs_consent_mop2di))
  (modules (mop2d_indexer))
  (libraries (lbvs_consent batteries dolog bitv parmap str zip minivpt cpm))))

(executables
 ((names   (cpu_bound_query))
  (modules (cpu_bound_query))
  (libraries (lbvs_consent batteries dolog bitv parmap str zip minivpt cpm))))

(executables
 ((names   (fdiv_option_test))
  (modules (fdiv_option_test))
  (libraries (lbvs_consent batteries dolog bitv parmap str zip minivpt cpm))))

(executables
 ((names   (cons_size_effect))
  (modules (cons_size_effect))
  (libraries (lbvs_consent batteries dolog bitv parmap str zip minivpt cpm))))

(executables
 ((names   (sdf_split))
  (modules (sdf_split))
  (libraries (lbvs_consent batteries dolog bitv parmap str zip minivpt cpm))))

(executables
 ((names   (sdf_get))
  (public_names (lbvs_consent_sdf_get))
  (modules (sdf_get))
  (libraries (lbvs_consent batteries dolog bitv parmap str zip minivpt cpm dokeysto_camltc))))
