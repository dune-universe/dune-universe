(jbuild_version 1)

(executable
 ((name basic)
  (libraries (open unix))))

(alias
 ((name runtest)
  (deps (
    basic.exe 
    "${ROOT}/examples/basic/ocaml logo.pdf"
    "${ROOT}/examples/basic/ocaml logo.png"
    "${ROOT}/examples/basic/ocaml logo.svg"
  ))
  (action (run ${<}))
))
