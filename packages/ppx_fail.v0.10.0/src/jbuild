(library
 ((name ppx_fail)
  (public_name ppx_fail)
  (kind ppx_rewriter)
  (flags (:standard -safe-string))
  (libraries (ppx_core ppx_driver ppx_here.expander))
  (preprocess (pps (ppx_metaquot ppx_driver.runner)))))

(jbuild_version 1)
