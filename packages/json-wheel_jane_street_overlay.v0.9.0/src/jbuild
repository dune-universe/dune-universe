(library
 ((name json_wheel_jane_street_overlay)
  (public_name json-wheel_jane_street_overlay)
  (libraries (core json-wheel))
  (preprocess (pps (ppx_jane ppx_driver.runner)))))

(jbuild_version 1)
