(library
  ((name bignum)
   (public_name bignum)
  (flags (:standard -safe-string))
   (libraries
    (core_kernel
     bigint
     typerep
     num
     zarith))
   (preprocess (pps (ppx_jane ppxlib.runner)))))


(jbuild_version 1)
