(library
 ((name bignum_test)
(flags (:standard -safe-string))
  (libraries (
    core
    bignum
    expect_test_helpers_kernel
    ))
  (preprocess (pps (ppx_jane ppxlib.runner)))))


(jbuild_version 1)
