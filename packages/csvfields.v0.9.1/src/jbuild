(library
 ((name        csvfields)
  (public_name csvfields)
  (flags (:standard -unsafe-string))
  (libraries
   (core
    csvlib
    sexplib
    xml_light))
  (preprocess (pps (ppx_jane ppx_driver.runner)))))

(jbuild_version 1)
