(library
 ((name ppx_conv_func)
  (public_name ppx_conv_func)
  (flags (:standard -safe-string))
  (libraries (base ppxlib))
  (preprocess (pps (ppxlib.metaquot ppxlib.runner)))))


(jbuild_version 1)
