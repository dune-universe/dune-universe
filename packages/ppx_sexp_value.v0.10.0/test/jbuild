(library
 ((name test_ppx_sexp_value_lib)
  (flags (:standard -safe-string))
  (libraries (core sexplib))
  (preprocess (pps (ppx_sexp_conv ppx_here ppx_assert ppx_sexp_value ppx_inline_test ppx_driver.runner)))))

(jbuild_version 1)
