(jbuild_version 1)

(executable
 ((name      make_FC_code)
  (modules   (make_FC_code))
  (libraries (str))))

(executable
 ((name      discover)
  (modules   (Discover))
  (libraries (base stdio configurator))))
