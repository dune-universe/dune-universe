(jbuild_version 1)

(library
 ((name        orsvm_e1071)
  (public_name orsvm_e1071)
  (modules (result utls svm svmpath))
  (libraries (dolog))))

(executables
 ((names     (test))
  (libraries (dolog batteries cpm minicli parmap))))
