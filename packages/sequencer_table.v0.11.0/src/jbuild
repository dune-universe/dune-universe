(library
 ((name sequencer_table)
  (public_name sequencer_table)
  (preprocess (pps (ppx_jane -check-doc-comments ppxlib.runner)))
  (flags (:standard -safe-string))
  (libraries (async))))


(jbuild_version 1)
