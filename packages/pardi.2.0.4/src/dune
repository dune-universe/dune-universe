
(library
  (name sec_chan)
  (modules DHKE crypto lz4)
  (libraries cryptokit batteries dolog lz4))

(executables
  (names        pardi client)
  (public_names pardi pardi_cli)
  (modules pardi flags utls mux demux sorted_queue client)
  (libraries batteries dolog parany minicli str sec_chan zmq))
