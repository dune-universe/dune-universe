(library
 ((name        toplevel_expect_test_types)
  (public_name toplevel_expect_test.types)
  (synopsis "Types for the output of `ocaml-expect -sexp`")
  (flags (:standard -safe-string))
  (libraries (core_kernel))
  (preprocess (pps (ppx_jane ppxlib.runner)))))


(jbuild_version 1)
