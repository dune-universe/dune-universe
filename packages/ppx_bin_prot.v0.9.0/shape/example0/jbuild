;; See code generated from [@@deriving bin_shape] without dependence on core.

(library
 ((name bin_shape_gen_example)
  (libraries ())
  (preprocess (pps (ppx_bin_prot ppx_driver.runner)))))

(alias ((name DEFAULT) (deps (example.ml.pp))))

(jbuild_version 1)
