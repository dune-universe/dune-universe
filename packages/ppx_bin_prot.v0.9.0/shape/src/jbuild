
(library
 ((name bin_shape_expand)
  (public_name ppx_bin_prot.shape-expander)
  (libraries (ppx_driver ppx_core ppx_type_conv ppx_here.expander))
  (preprocess (pps (ppx_metaquot ppx_driver.runner)))))

(jbuild_version 1)
