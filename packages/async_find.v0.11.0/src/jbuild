(library
 ((name async_find)
  (public_name async_find)
  (flags (:standard -safe-string))
  (libraries (async
                    core))
  (preprocess (pps (ppx_jane ppxlib.runner)))))


(jbuild_version 1)
