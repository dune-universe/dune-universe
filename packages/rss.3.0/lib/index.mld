{0 Rss}

OCaml-RSS is a small OCaml library providing functions to parse and
print RSS 2.0 files. The parser can also parse some RDF files, but
some fields are not taken into account.

It supports {{:https://cyber.harvard.edu/rss/rss.html#extendingRss}extended RSS}.

It is available in {{:https://opam.ocaml.org/packages/rss/}Opam (package [rss])}.

{1:api API}

The API consists in this module: {!Rss}.
