build-and-test:
  image: ocaml/opam2:alpine-3.10-ocaml-4.08
  script:
    - opam pin --no-action secp256k1-internal .
    - opam depext secp256k1-internal
    - opam install --deps-only -t .
    - dune build
    - dune runtest
    - opam install .
