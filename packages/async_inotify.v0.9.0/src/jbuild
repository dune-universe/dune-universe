(library
 ((name async_inotify)
  (public_name async_inotify)
  (libraries (async
              async_find
              core
              inotify))
  (preprocess (pps (ppx_jane ppx_driver.runner)))))

(jbuild_version 1)
