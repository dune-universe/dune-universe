(executables
 ((names (example))
  (libraries (virtual_dom async_js core_kernel async_kernel js_of_ocaml))
  (preprocess (pps (ppx_js_driver ppx_sexp_conv ppx_driver.runner)))
  ))

(jbuild_version 1)
