
(library
  (name molenc)
  (public_name molenc)
  (modules ap_types atom_env fingerprint fpMol mini_mol MSE_mol myList
           node piEltHA scale utls WMH norm)
  (libraries batteries dolog bst))

;; installed executables / public targets
(executables
  (names encoder decoder filter butina pubchem_decoder uniq)
  (public_names molenc_e molenc_d molenc_filter molenc_cluster
                molenc_pubchem_decoder molenc_uniq)
  (modules encoder decoder filter butina pubchem_decoder uniq)
  (libraries molenc bst batteries dolog minicli parmap))

;; never installed executables
(executables
  (names fp_test wmh_test wmh_bench wmh_unit_test)
  (modules fp_test wmh_test wmh_bench wmh_unit_test)
  (libraries molenc batteries dolog minicli))
