
(library
  (name        lz4_chans)
  (public_name lz4_chans)
  (modules lz4_chans)
  (wrapped false)
  (libraries dolog unix))

(executables
  (names test)
  (modules test)
  (libraries dolog lz4_chans))
