(jbuild_version 1)

(library
 ((name        integration1d)
  (public_name integration1d)
  (synopsis "Integration of functions of one variable.")
  (wrapped false)
  (preprocess (action (run ${bin:camlp4of} ${<})))))
