(library (
  (name async_shell)
  (public_name async_shell)
  (flags (:standard -unsafe-string))
  (libraries (
    async
    core
    core_extended
  ))
  (preprocess (pps (ppx_jane ppx_driver.runner)))
))

(jbuild_version 1)
