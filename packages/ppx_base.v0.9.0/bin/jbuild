(executables
 ((names (main))
  (libraries (ppx_base ppx_driver))
  (link_flags (-linkall))
  (preprocess no_preprocessing)))


(install
 ((section bin)
  (files ((main.exe as ppx-base)))))

(jbuild_version 1)
