(env
  (dev
     (flags (:standard -warn-error -A))))

(library
 (name srt_stubs)
 (public_name srt.stubs)
 (libraries srt.types ctypes.stubs sys-socket))

(rule
 (targets srt_generated_types.ml)
 (deps    (:gen ../generator/gen_types_c))
 (action  (system "%{gen} > %{targets}")))
