(ocamllex (plaLex))
(library
  (
      (name pla_ppx)
      (public_name pla.ppx)
      (kind ppx_rewriter)
      (libraries (
         compiler-libs
         ocaml-migrate-parsetree))
   )
)

(jbuild_version 1)