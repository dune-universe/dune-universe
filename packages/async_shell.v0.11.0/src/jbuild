(library (
  (name async_shell)
  (public_name async_shell)
  (flags (:standard -safe-string))
  (libraries (
    async
    core
    core_extended
  ))
  (preprocess (pps (ppx_jane ppxlib.runner)))
))


(jbuild_version 1)
