(library
  (name            apero_time)
  (public_name     apero-time)
  (wrapped        true)
  (libraries    apero-core lwt lwt.unix logs logs.lwt logs.fmt logs.cli fmt fmt.cli fmt.tty)
  (preprocess (pps ppx_deriving.show ppx_deriving.ord lwt_ppx)))

