(jbuild_version 1)

(library
 ((name        sslconf)
  (public_name sslconf)
  (synopsis    "an ocaml version of openssl NCONF")
  (libraries (astring bytes sexplib rresult))
  (preprocess (pps (ppx_sexp_conv)))
  (flags (:standard -w +A-4-41-42-44))
 ))
