
let abs(x : real) : real = 
    if x > 0.0 then x else -x


node main(T : real = 10.0; Heat_on : bool) returns ( 
--	T1 : real;
	T2 : real = 10.0; 
	T3 : real = 10.0) =

-- assert abs(T-T1) < 0.5 in
assert abs(T-T2) < 0.5 in
assert abs(T-T3) < 0.5 in

   loop true
