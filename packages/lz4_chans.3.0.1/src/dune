
(library
  (name        lz4_chans)
  (public_name lz4_chans)
  (modules lz4_chans)
  (wrapped false)
  (libraries dolog unix str))

(executables
  (names test)
  (modules test)
  (libraries lz4_chans))
